import type { ActivityEntry } from './types';

/**
 * What is actually being worked on now. Short, specific, and worth updating often.
 * A stale "Currently" block is worse than no "Currently" block.
 */
export const ACTIVITY: ActivityEntry[] = [
  // Only Caleb can fill this in. What is being worked on right now is not
  // derivable from a repository, and guessing at it is how this section ends up
  // claiming work that is not happening. Empty until it is written.
];
