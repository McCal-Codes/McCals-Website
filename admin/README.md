# Archived Admin Prototype

The root `admin/` directory is no longer an active application surface.

Its former standalone blog CMS prototype has been archived under:

- `_archived/legacy-standalone-blog-cms-2026-04-06/`

The active internal admin direction now lives in:

- `sites/mcc-cal-admin/`

Use the archived files only as local historical reference when evaluating future Git-backed editorial tooling.
